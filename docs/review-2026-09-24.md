# Code review: issues and fixes (2026-09-24)

Scope: the whole fork at `cam_wip` `bd0b72b` — the C++ data driver, readback
node, `smartmicro_processing`, the view scripts, the RViz panels,
`umrr_ros2_msgs`, `smartmicro_description`, Docker and CI. Items already
tracked in [ROS 2 driver improvements](ros2-improvements.md) are not repeated
unless the review found a specific defect beyond them.

Each item has an ID used in commit messages. **Status** is updated as fixes
land: `open`, `fixed`, `partial` (with a note), or `deferred` (with a reason).
**Origin**: `upstream` means inherited from smartmicro; `fork` means
introduced on this fork.

## Correctness

| ID | Where | Defect | Fix | Origin | Status |
|----|-------|--------|-----|--------|--------|
| C1 | `umrr_ros2_driver/src/smartmicro_radar_node.cpp:963` | A CAN sensor with a missing or unknown `pub_type` logs "Unkwon publish type!" at INFO, creates no publisher, but still registers the SDK callback. The first frame dereferences a null publisher on an SDK thread (segfault). `pub_type` defaults to `""`. An unknown `model` string also silently registers nothing. | Throw `std::invalid_argument` as the port path does (`:466`); validate `link_type`/`model`/`pub_type` together at startup and reject unknown models. | upstream | fixed (link/pub_type/model validated at startup; unknown values throw) |
| C2 | `smartmicro_radar_node.cpp:4721, 4828, 4935, 5043, 5150, 6080, 6176` | CAN object clouds fill `heading` from `GetHeadingDeg()` (degrees) while port object clouds use `GetHeading()` (radians). Same field, factor 57.3 apart; REP-103 requires radians. | Convert to radians in the CAN callbacks after confirming the SDK unit. | upstream | fixed (SDK/UIF: HeadingDeg is `_deg`; converted to rad) |
| C3 | `smartmicro_radar_node.cpp:3308-3310` | umrr9f v2.0.0 target header maps `acquisition_tx_ant_idx ← GetAcquisitionTxAnt()` and `acquisition_cf_idx ← GetAcquisitionTx()`, but the SDK header documents `GetAcquisitionTx` as the TX antenna index and `GetAcquisitionTxAnt` as the centre-frequency index. Only outlier among 29 port-target callbacks. | Confirm against the vendor interface PDF, then swap. | upstream | fixed (UIF XML: offset 6 `AcquisitionTx`=TX antenna, 8 `AcquisitionTxAnt`=CF; getter names wrong) |
| C4 | `umrr_ros2_driver/src/update_service.cpp:100-104` | Firmware wait ends when all bytes are transferred even if the status is still `RUNNING`; `RUNNING` is then reported as `kUnknownError` and the busy flag cleared, so a second update can start during flashing. The wait (up to 5 min) also blocks the single-threaded executor, stalling diagnostics and all other services. | Wait only for `status != RUNNING`; run the service in its own callback group / off the diagnostics path. | upstream | fixed (waits for status != RUNNING; deferred-response service, worker thread) |
| C5 | `smartmicro_radar_node.cpp:1439, 1450` | `set_radar_mode` parses with `stoul`/`stof`: `"-1"` → `0xFFFFFFFF`, `"5000000000"` wraps, `"12abc"` → 12, `"nan"` accepted; all reported as success. | Full-consumption `from_chars` with range checks and finite-float check (as in `umrr96_tuning.hpp`). | upstream | fixed (`parse_mode_value`, full `from_chars`, range/finite checks) |
| C6 | `smartmicro_radar_node.cpp:1328, 1386, 1529, 1592, 1646, 1746` | Service `sensor_id` validation iterates all 10 slots; unused slots have id 0, so `sensor_id: 0` passes. | Iterate only the configured sensors. | upstream | fixed |
| C7 | `smartmicro_radar_node.cpp:1815` | `get_radar_mode` error text lists value types as 0=u32…3=float; the switch implements 0=float, 1=u32, 2=u16, 3=u8. | Correct the message. | upstream | fixed |
| C8 | `include/umrr_ros2_driver/smartmicro_radar_node.hpp:1460-1545` | 30 namespace-scope globals (`m_services`, `data_*`) defined non-inline in an installed header: a second translation unit fails to link, and SDK handles are destroyed at static-destruction time in unspecified order relative to the SDK's own statics. | Move them into the `.cpp` (or into the node). | upstream | fixed (node members; UI handles type-erased per configured model) |
| C9 | `include/umrr_ros2_driver/sdk_callback_gate.hpp` | `SdkCallbackGate::wrap` does not catch, so any exception in a data callback (`bad_alloc`, builder `length_error`, rclcpp) propagates into vendor threads and likely terminates the process. | Catch `std::exception`/`...` inside `wrap` and log (throttled). | fork | fixed (caught, counted, logged ≤1/s) |
| C10 | `smartmicro_radar_node.cpp:4725` and other CAN object callbacks | CAN object id cast `uint16_t` → `int16_t`; ids ≥ 32768 become negative. | Document, or widen in a schema bump. | upstream | fixed (documented in `umrr_ros2_driver/doc/interfaces.md`; type kept) |
| C11 | `smart_rviz_plugin/src/smart_services.cpp:514, 564, 616, 668` | `spin_until_future_complete` with no timeout on the GUI thread (after a blocking 2 s `wait_for_service`). A stalled server freezes all of RViz. | Async request polled from a `QTimer` with a deadline and `remove_pending_request` (Umrr96Config pattern). | upstream | fixed |
| C12 | `smart_services.cpp:512, 551, 604, 605, 659` | Uncaught `std::stoi` on user text: "abc" or an out-of-range value aborts RViz; float `SendCommand.value` truncated to int; negative ids wrap. | `QString::toUInt(&ok, 0)` / `toFloat(&ok)`; report parse errors in the panel. | upstream | fixed |
| C13 | `smart_rviz_plugin/include/smart_rviz_plugin/smart_services.hpp:98`, `smart_services.cpp:299` | Sensor-type tables are loaded from `QDir::currentPath() + "/src/smartmicro_ros2_radars/..."`; empty unless RViz starts from the workspace root. Failure logged only at INFO. | Resolve from an installed share directory; show the error in the panel. | upstream | fixed |
| C14 | `smart_rviz_plugin/src/smart_recorder.cpp:34`, `smart_status.cpp:23` | Topics discovered once in the constructor, when the graph is usually still empty; never refreshed. `umrr96_live.launch.py` starts RViz with the driver, so dropdowns stay empty. | Periodic refresh (as `smart_faults.cpp` does); subscribe lazily to the selected topic. | upstream | fixed |
| C15 | `smart_rviz_plugin/src/smart_download.cpp:117-135` | Detached worker thread captures `this`; a late reply after panel destruction is a use-after-free. `[this, e]` slices the exception (UI shows "std::exception"); `!toInt()` rejects id 0 and accepts negatives. | Drop the detached thread; handle the reply on the panel's executor and post through a `QPointer`; parse ids strictly. | upstream | fixed |
| C16 | `smart_rviz_plugin/src/smart_faults.cpp` `refresh_topic_list` | When the selected topic disappears the combo resets to index 0 with signals blocked, but the subscription stays alive; UI shows "Select…" while tables keep updating. | Call `on_topic_selected(0)` when the previous selection vanishes. | upstream | fixed |
| C17 | Recorder, Status, Faults, Services, Download panels | Fixed node names (`smart_service_gui`, …): two instances of a panel create duplicate node names. | Instance counter suffix (as Umrr96Config does). | upstream | fixed |
| C18 | `smartmicro_processing/smartmicro_processing/accumulation_node.py` + `accumulation.py:62` | `max_pose_translation_step` (1.0 m) is compared with the displacement between consecutive processed scans, not scaled by dt: above 10 m/s at 10 Hz (fit allows 15 m/s), or after dropped scans, the evidence is wiped every scan with no log. | Limit = `step + max_speed·dt`; throttled warning on reset. | fork | fixed |
| C19 | `umrr_ros2_driver/scripts/umrr96_views.py:216` | Stale-input "clear" of `filtered_targets_0` deep-copies `last_cloud`, republishing an old stamp (TF filters drop it; monotonic checks downstream break). | Stamp clears with the current time. | fork | fixed |
| C20 | `smartmicro_processing/smartmicro_processing/node.py:309-312` | Empty clear clouds are stamped with the processing node's `now()`; the accumulation node then rejects later, older-stamped real clouds as non-monotonic, and the empty clouds refresh its receipt watchdog, hiding upstream staleness. | Accumulation ignores `width == 0` clouds for stamp/receipt bookkeeping; clear clouds carry the last accepted input stamp. | fork | fixed |
| C21 | `node.py`, `accumulation_node.py`, `umrr96_views.py` parameter declarations | Parameters declared from float defaults: an integer override (`max_range: 120`, `publish_hz:=5`) raises `InvalidParameterTypeException` at startup. | Accept int or double and coerce. | fork | fixed |
| C22 | `node.py:415-427`, `umrr96_views.py:400-412` | Only `KeyboardInterrupt` caught; `ExternalShutdownException` under `ros2 launch` gives a traceback; `ok()`/`shutdown()` pair races the signal handler. | Catch both; `rclpy.try_shutdown()`. | fork | fixed |
| C23 | `accumulation_node.py:285-296`, `evidence_cloud.py` | `source_point_index` indexes the accumulation *input* (`doppler_inliers`), not the driver cloud; joining back to `port_targets_0` picks wrong detections. | Document the index's reference cloud precisely. | fork | fixed (documented) |
| C24 | `smartmicro_processing/smartmicro_processing/doppler.py:221-227` | Covariance mixes unweighted residual variance with final-iteration IRLS weights. Conservative, but inconsistent for fusion. | Weighted residual variance with weights at the returned velocity. | fork | fixed |
| C25 | `umrr_ros2_driver/src/smartmicro_radar_readback_node.cpp:348-350` | A timeout increments both `timeouts_` and `failed_requests_`. `timeout_ms` may be up to 30 s while the panel deadline is 5 s. | Count once; bound `timeout_ms` to the panel deadline (or match). | fork | fixed (counted once; `timeout_ms` ≤ 4000) |
| C26 | `smart_rviz_plugin/custom_can_sender.py:39-50, 85` | Only `ValueError` caught: a CAN/OS error stops the periodic loop silently; Bus reopened per message; deprecated `bustype=`; 29-bit ids accepted but sent as 11-bit. | Catch broadly, reschedule in `finally`, keep one Bus, `interface=`, set `is_extended_id` from the id. | upstream | fixed |

## Performance

| ID | Where | Issue | Fix | Origin | Status |
|----|-------|-------|-----|--------|--------|
| P1 | `smartmicro_radar_node.cpp`, 69 callbacks; `:4044` | `std::cout << … << std::endl` on every frame on SDK receive threads (all models except UMRR-96), plus an `RCLCPP_INFO` per fault report. | Remove / debug-throttle. | upstream | fixed |
| P2 | `update_service.cpp` `UpdateCallback` | INFO log per firmware block. | Throttle. | upstream | fixed (≤1 progress line/s) |
| P3 | Driver publishers | Clouds published by `const&` from stack locals; deep copy under intra-process comms. | Publish `unique_ptr` with `std::move`. | upstream | fixed |
| P4 | `smartmicro_processing/smartmicro_processing/cloud.py:78-82` | `subset_cloud` copies point by point (≈3.7 ms / 4096 points), four times per message. | NumPy row gather. | fork | fixed |
| P5 | `accumulation.py:118-149`, `evidence_cloud.py:20-24` | Per-point voxel keys, per-cell dict snapshot and per-sample packing (≈25 ms per publish at the cap); built even with no subscribers. | Vectorise; skip building clouds with no subscribers. | fork | fixed |
| P6 | `umrr_ros2_driver/scripts/umrr96_filter.py` mapping mode | O(N·M) Python comparison against the previous two scans; seconds per scan near the 4096 cap. | Spatial hash / vectorised gate. | fork | fixed |
| P7 | `node.py`, `accumulation_node.py` | `DiagnosticArray` published per message and per 100 ms watchdog tick (10–20 Hz); rejections publish four empty clouds each time with no log. | ≈1 Hz diagnostics; throttled warning for `unexpected_frame`; clear outputs on state transition only. | fork | fixed |
| P8 | `smart_rviz_plugin/src/smart_recorder.cpp:122-144, 245-292` | Recorder decodes a hard-coded camera topic on the GUI thread and republishes it; tables rebuilt row by row per message; unbounded recording; temporary executor every 50 ms. | Remove the camera path; `setRowCount` once with updates disabled; bound history; persistent executor. | upstream | fixed |

## ROS 2 / Nav2 conventions

| ID | Where | Deviation | Fix | Origin | Status |
|----|-------|-----------|-----|--------|--------|
| S1 | `accumulation_node.py:199, 258`, `ACCUMULATION.md` | Pose-compensated output has `frame_id: odom` and is stamped at publish time. Nav2's ObstacleLayer would raytrace from the odom origin unless `sensor_frame` is set, and TF lookups at a future stamp can fail. | Stamp with the newest contributing scan; ship a Nav2 costmap snippet (`sensor_frame`, `clearing: false`, height limits) and recommend `observation_persistence` on the sensor-frame cloud. | fork | fixed |
| S2 | `umrr_ros2_driver/CMakeLists.txt:28, 38, 55, 98-109, 186` | Configure-time writes into `CMAKE_INSTALL_PREFIX`; absolute install-tree include path exported; hard-coded x86_64 SDK dir; generic SDK libs installed into shared `lib/`; unconditional `-g`. | Generate into the build dir and `install()`; generator-expression includes; pick SDK dir by processor; SDK libs under `lib/${PROJECT_NAME}` with RPATH; drop `-g`. | upstream | fixed (SDK libs in `lib/umrr_ros2_driver` + RPATH; x86_64 only, clear error otherwise; SDK aborts on `shared_lib_path` ≥160 chars, so nodes pass a short alias) |
| S3 | `smartmicro_processing/config/umrr96_processing.yaml:1`, `umrr_ros2_driver/param/radar.params.umrr96_38553.yaml:3, 34, 44` | Parameter files keyed to absolute node names; settings silently ignored once a node is namespaced. | `/**:`-style keys or per-node files; `namespace` launch argument. | fork | partial: processing YAML/launch done; driver param files are the driver lane |
| S4 | `node.py:49`, `accumulation_node.py:45`, `umrr96_views.py:119`, processing/accumulation launch files | Inputs selected through `input_topic` parameters with absolute defaults instead of relative names plus remapping. | Relative subscription names, remapped in launch; keep parameter for compatibility. | fork | partial: processing launches remap; `umrr96_live.launch.py` should remap views input |
| S5 | `smartmicro_radar_node.cpp:259-262, 438-462, 946-961` | Hard-coded `smart_radar/` prefix and sensor index in topic names; the node cannot be relocated by namespace alone. | Keep existing names by default (compatibility), document namespace use; tracked with backlog item 1. | upstream | fixed (names unchanged; namespace use documented) |
| S6 | Python nodes; `smartmicro_radar_node.cpp:257, 266` | Hand-rolled `/diagnostics` at sensor rate; driver `hardware_id` is the generic "smartmicro" / numeric client id. | `diagnostic_updater` at ≈1 Hz (see P7); `hardware_id` = model@ip. | mixed | partial: Python nodes rate-limited (~1 Hz, steady clock); driver `hardware_id` open; partial (driver `hardware_id` = model@ip; Python nodes are another lane) |
| S7 | `umrr_ros2_driver/scripts/umrr96_views.py:147` | The only filtered cloud usable for navigation is produced by the visualization process. | Allow running the views node headless (images/markers only with subscribers); document. | fork | fixed (documented in the views module; bring-up doc not yet updated) |
| S8 | `umrr_ros2_driver/CMakeLists.txt:289-294` | `umrr96_filter.py`/`umrr96_fan_image.py` installed as loose files imported via `sys.path`. | Install as a Python package with `ament_python_install_package`. | fork | fixed |
| S9 | `package.xml` files | Driver `<depend>`s `rclpy`/`visualization_msgs` (exec only) and exec-depends `rviz2`/`smart_rviz_plugin` (Qt on headless robots); msgs declares unused `geometry_msgs`/`sensor_msgs`; RViz plugin uses `sensor_msgs` undeclared and declares unused deps; non-SPDX licenses; mismatched build/install include interface in the plugin. | Correct dependency kinds, remove unused, SPDX `Apache-2.0`. | mixed | partial: `smart_rviz_plugin` fixed (sensor_msgs/ament_index_cpp declared, unused deps removed, SPDX, include interface); driver/msgs manifests belong to other lanes; partial (driver + msgs fixed; rviz2/smart_rviz_plugin kept as exec_depend because the live launch starts RViz; plugin is another lane) |
| S10 | `umrr_ros2_driver/CMakeLists.txt:249-285`; processing and description packages | Most `ament_lint` checks excluded; the 6.4 kLOC node, processing and description packages not linted. | Exclude only vendored SDK files; enable flake8/pep257/xmllint where the code passes. | mixed | partial: flake8/pep257/xmllint/lint_cmake enabled for `smart_rviz_plugin` and `smartmicro_description`; plugin C++ not yet uncrustify/cpplint clean; driver/processing in other lanes; processing package and driver Python linted; driver C++ open; partial (driver: cppcheck/cpplint/uncrustify/flake8/pep257/lint_cmake/xmllint on own files; SDK excluded; cpplint include_order/copyright filtered; SPDX-only files not in copyright check) |
| S11 | `umrr_ros2_msgs` | Misspelled `umambiguous_speed`, `occurence_count`; units undocumented; `SetMode.value_types` has no named constants; point-field units and Doppler sign undocumented. | Add correctly spelled fields (old kept, deprecated); unit comments; constants; document PointCloud2 fields. | upstream | fixed |
| S12 | Driver outputs | No standard `radar_msgs/RadarScan` option. | Optional `publish_radar_scan` output for port targets. | upstream | fixed (optional; built and tested against an extracted ros-lyrical-radar-msgs .deb, not installed system-wide) |
| S13 | `node.py:27`, `accumulation_node.py:227`, `umrr96_views.py:138` | Parameters lack descriptions and ranges. | `ParameterDescriptor` with description and ranges. | fork | fixed |
| S14 | `smartmicro_radar_readback_node.cpp:102, 361` | Readback node is not a composable component. | Accept `NodeOptions`, register with `rclcpp_components`. | fork | fixed (component + standalone exe with own main) |
| S15 | `umrr96_live.launch.py`, `radar.launch.py`, `umrr96_description.launch.py:184` | Always starts RViz (exit kills the driver); no `namespace`/`use_sim_time`; no params-file argument in `radar.launch.py`; absolute `/umrr96/robot_description` remap. | `rviz`, `namespace`, `use_sim_time`, `params_file` arguments; relative remap. | mixed | partial: description launch uses a relative `robot_description` remap; driver launch arguments in another lane; partial (driver launches done; description launch is another lane) |
| S16 | `.github/workflows/dockerbuild.yml`, `Dockerfile`, `docker-compose.yml` | CI triggers only on `master`, `checkout@v2`, no Lyrical, foxy default; Dockerfile installs the removed point-cloud wrapper and never runs `rosdep`; network-simulator tests require `-DSMARTMICRO_NETWORK_TESTS=ON`, which CI never sets; root user, no apt cleanup. | Jazzy/Lyrical matrix, `rosdep install`, drop wrapper, set the test flag, non-root build, apt hygiene. | mixed | partial: CI on push (master, cam_wip)/PR, checkout@v4, Humble/Jazzy/Lyrical matrix (Foxy dropped: panels need FutureAndRequestId), rosdep-driven non-root Dockerfile without the wrapper, `-DSMARTMICRO_NETWORK_TESTS=ON`; Humble image build and plugin build verified locally, workflow itself not run; two Humble test failures allowed |
| S17 | `smart_extract.sh` | No checksum on the SDK download; `wget -c` resumes stale files; declining the licence exits 0. | Pin and verify sha256; no `-c`; exit 1 on decline. | upstream | fixed (sha256 pinned from the published 3.13.0 archive) |
| S18 | `radar.params.umrr96_38553.yaml` | Default `params_file` of the live launch hard-codes a host interface and IP. | Keep as the documented bench file; make the launch argument explicit and document. | fork | fixed |

## Maintainability

| ID | Where | Issue | Fix | Origin | Status |
|----|-------|-------|-----|--------|--------|
| M1 | `smartmicro_radar_node.cpp` data callbacks | ~4,300 lines across 71 near-identical per-model callbacks; divergence produced C2 and C3. | Templated per-stream publishers with per-model traits. | upstream | fixed (`stream_codecs.hpp` + `sensor_model_traits.hpp`; mapping pinned by `test_stream_mapping`; CAN targets of 7 models now also fill SDK-provided `acq_ts_fraction`/`cycle_time`) |
| M2 | `smartmicro_radar_node.cpp:476-938, 973-1318` | ~50 registration blocks; failures logged at INFO. | Table-driven registration; failures are errors. | upstream | fixed (table-driven; registration failures throw) |
| M3 | Driver | Dead members/constants/includes; unused `client_id` parameters; `m_shutdown_requested` redundant with the callback gate. | Remove. | upstream | fixed |

## Verification

Record here, per fix batch, the build and test commands run and their results.

### Merged branch `review-fixes-20260924`

- Clean Release build of all five packages on Lyrical in an isolated
  environment (only `/opt/ros/lyrical` sourced): passes.
- `colcon test`: 345 tests, 27 skipped (cppcheck disables itself on 2.19),
  1 failure: `umrr96_panel_smoke`, pre-existing and load-sensitive under
  parallel package testing; it passes 3/3 when run alone.
- Lane results: driver 87 → 186 tests, processing/Python 135 → 204,
  RViz/description 16 → 37; all green in their lanes.
- Python processing: the 808-scan recorded bag gives byte-identical outputs
  before and after; `subset_cloud` 2.09 → 0.45 ms, accumulation snapshot
  23.5 → 2.3 ms, mapping filter at the cap 387 → 7.4 ms.
- Live UMRR-96 (192.168.11.11, read-only, no settings changed), 30 s:
  252 clouds at 8.33 Hz (baseline 8.35 Hz), monotonic stamps, frame `umrr96`,
  all 18 point fields unchanged, raw-quality topic present, diagnostics `OK`
  with `hardware_id` `umrr96_v1_2_2@192.168.11.11`, clean exit (status 0).

Behaviour changes for downstream users: CAN object `heading` in radians;
umrr9f v2.0.0 acquisition indices swapped to match the vendor XML; new
`unambiguous_speed`/`occurrence_count` fields (message hashes change; old
fields still filled); SDK libraries install to `lib/umrr_ros2_driver/`;
unknown model/link/pub types fail at startup; parameter keys are
`/**/<node>:`; readback `timeout_ms` ≤ 4000; `radar_msgs` output is compiled
only when `radar_msgs` is installed.

## Follow-up review (2026-09-25)

Scope: everything after `ff352bc` (ghost rejection, tracker, Nav2 obstacle
evidence, tracking launch) plus a second pass over the refactored driver, the
RViz plugin, launch files and CI. Status notes above that were stale: S3 is
done (every parameter file uses `/**/` keys); S4's remaining gap is only the
audit CLIs' absolute defaults (`audit.py`, `accumulation_audit.py`).

| ID | Where | Defect | Fix | Status |
|----|-------|--------|-----|--------|
| C27 | `smartmicro_radar_node.cpp` `radar_command` | `SendCommand.value` (float32) cast to the SDK's `uint32_t`: −1, NaN or 1e10 is undefined behaviour, 1.7 becomes 1; commands include reset and EEPROM saves. | `parse_command_value`: finite whole numbers 0..2^24 only. | fixed |
| C28 | `smartmicro_radar_node.cpp` `ip_address` | New IP saved to EEPROM unvalidated: 0.x, loopback, multicast/broadcast leave the sensor unreachable after restart. | `validate_sensor_ipv4`. | fixed |
| C29 | `smartmicro_radar_node.cpp` `firmware_download` | `Busy()` turns true only inside the worker: a second request in that window joins the running worker on the executor (blocking diagnostics/services for the whole flash), then starts a second update. | `firmware_active_` set before the worker starts, cleared at its exit. | fixed |
| C30 | `umrr_ros2_driver_py/views.py` `publish` | Subscriber count read twice: a first `density_cells` subscriber between the reads raised `UnboundLocalError` and stopped the views node (and `filtered_targets_0`). | Read each count once. | fixed |
| C31 | `smartmicro_radar_node.cpp` `setup_diagnostics` | `diagnostics.stale_timeout: 5` (integer) failed startup (C21 for the C++ node). | Dynamic typing; range checked. | fixed |
| C32 | `smartmicro_processing/node.py` `clear_clouds` | Any single rejected scan (stale/non-monotonic stamp, wrong frame) rebuilt the tracker and its background. | Rejections clear outputs only; state resets only on a backwards clock; tracker drops tracks after a gap > `max_coast`; persistence restarts after a gap > `stale_timeout`. | fixed |
| C33 | `tracker.py` `BackgroundModel` | Occupancy averaged from zero: a wall became background only after 21 s, and until then the shadow rule dropped all static returns beyond a tracked person (38.8% of known structure kept on `walk`). | Bias-corrected occupancy, `background_warmup` 3 s, shadow rule off until ready, `background_threshold` 0.3: 80.6% kept. | fixed |
| C34 | `node.py`, `tracker.py` | Background and shadow rule assume a scene-fixed sensor; on a moving sensor everything beyond a track is novel and dropped. | Background reset while the fitted speed exceeds `sensor_moving_speed` for 3 scans. Running tracker/persistence in a fixed frame with TF remains open. | partial |
| C35 | `node.py` failed fit | Tracks were not stepped or published on a failed Doppler fit (stale markers, no `tracked_objects`). | `tracker.coast()`; tracks published and kept in `obstacles`. | fixed |
| C36 | `ghosts.py`, `tracker.py` `is_ghost` | Same-speed ghost rules ignore bearing and sign: a second real mover more than 1.5 m farther with similar (or double) \|speed\| is dropped. Sign gating was measured and rejected (ghosts removed 93.7% → 83.5%). | Documented; needs a two-person capture. | open |
| C37 | `umrr96_live.launch.py` description include | Unscoped include inherited `rviz:=true` (second, unmanaged RViz with `publish_description:=true`); `sensor_name` not forwarded (two radars share `umrr96_link`). | Pass `rviz: false`, `description_sensor_name`, `use_sim_time`. | fixed |
| C38 | driver and readback diagnostics | Two namespaced radars published identical status names (`smart_radar: Target stream 0`), so one radar's STALE hid behind the other's OK. | `diagnostic_updater.use_fqn` when a namespace is set (names unchanged otherwise). | fixed |
| C39 | RViz panels, all `.rviz` configs | Absolute `/smart_radar/...` endpoints (and `use_global_arguments(false)` in Umrr96Config): with `namespace:=` the displays and panels stay empty. | Relative names; a namespace panel property. | open |
| C40 | `.github/workflows/dockerbuild.yml`, `docker-compose.yml` | `colcon test-result --all` makes the Humble job red regardless of `continue-on-error`; `colcon test` without `--return-code-on-test-failure`; `docker compose up` ignores the test exit code; no simulator healthcheck; workflow never run. | `docker compose run --rm`, return codes, exclude known Humble failures. | open |
| C41 | RViz panels | One `spin_some` per 50 ms tick handles at most one message per subscription: 20 msg/s per topic. The Recorder silently drops frames above that; the UMRR-96 now streams 18.18 Hz. | Bounded `spin_all`. | open |
| C42 | `umrr96_panel_smoke` | Load-sensitive: heartbeat count assumes ≥ 20 Hz Qt timers; panel deadlines hard-coded 3–5 s, ignoring `request_timeout_ms`. | Honour `request_timeout_ms`; check max gap, not count. | open |
| C43 | RViz SIGSEGV on SIGINT | Also seen with configs that load none of these panels and with bare `rviz2` (apport); not attributable to `smart_rviz_plugin`. | Retrace the retained core. | open (upstream) |
| C44 | `umrr96_config.cpp` firmware identity | A firmware-identity timeout calls `fail()`, clearing read settings and disabling Apply. | Report without discarding settings. | open |
| S19 | `package.xml` (driver, processing) | Headless installs pull in RViz/Qt through `exec_depend` (S9 remainder). | Separate viz/bringup package. | open |
| P9 | `fan_image.py` | Every tick redraws the full frame even without a new scan (likely most of the 19% CPU). | Cache the background, draw occupied bins, skip unchanged input. | open |
| M4 | `SdkCallbackGate` | `exception_count()` unused: a stream that throws on every frame still reports OK. | Add to the Target stream diagnostic. | open |

Feature: `smart_radar_readback.startup_can_target_output` (default 0) switches the
UMRR-96 CAN target list off at startup and confirms it by readback; see
[bring-up](umrr96-bringup.md#read-parameters-and-status).

### Verification (2026-09-25)

- `smartmicro_processing`: 101 pytest cases (95 → 101), flake8/pep257 clean.
  Replay scoring of `walk`/`scoot`/`static` in
  [filtering](umrr96-filtering-20260924.md#background-warm-up-and-moving-sensor-guard-2026-09-25).
- `umrr_ros2_driver`: Release build; `colcon test` 192 tests, 0 failures,
  27 skipped (cppcheck); new gtests for command values and IP validation, and a
  readback test that the startup CAN write is sent and retried.
- Live UMRR-96 (192.168.11.11): after a power cycle, CAN output read back as 1;
  writing 0 raised the stream from 8.33 to 18.18 Hz. A readback node from the new
  build confirmed "CAN target output off" 1.1 s after startup on its first
  attempt. Processing on the live stream: valid fits, background ready, 8.6 ms
  per scan.
